---
name: Compose
shorthand: compose
category: strategy
tags: []
summary: The agent builds a whole from existing, well-defined parts by inventorying sub-components, wiring them through explicit seams, and preferring reuse over reinvention.
composes_with: [decompose, normalize, cache]
opposes: [generate]
---

# Compose (`compose`)

When this macro is invoked, the agent builds a whole from existing, well-defined parts. It inventories available sub-components, wires them together through explicit seams, and prefers reuse over reinvention. The agent makes the assembly structure visible — which parts were combined and how they connect.

## Examples

TODO: Add concrete examples from real usage.
